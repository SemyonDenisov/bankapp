spring:
  application:
    name: accounts-microservice
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_rk
    username: postgres
    password: 1
  sql:
    init:
      mode: always
  jackson:
    property-naming-strategy: SNAKE_CASE
logging:
  level:
    org:
      springframework:
        security: DEBUG
