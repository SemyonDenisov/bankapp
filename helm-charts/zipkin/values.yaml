replicaCount: 1

image:
  repository: openzipkin/zipkin
  tag: "3.5"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 9411

resources:
  requests:
    memory: "512Mi"
    cpu: "500m"
  limits:
    memory: "1Gi"
    cpu: "1"

ingress:
  enabled: false
