apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-ui-microservice
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: front-ui-microservice
  template:
    metadata:
      labels:
        app: front-ui-microservice
    spec:
      containers:
      - name: front-ui-microservice
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 8089
